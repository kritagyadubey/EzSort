[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ezsort"
version = "0.1.0"
description = "A safe, lightweight Python CLI that automatically organizes messy folders into categories."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
authors = [
    {name = "Kritagya Dubey"},
]
keywords = ["file-organizer", "cli", "cleanup", "downloads", "organization"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Utilities",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[project.scripts]
ezsort = "ezsort.cli:main"

[project.urls]
Homepage = "https://github.com/kritagyadubey/EzSort"
Repository = "https://github.com/kritagyadubey/EzSort"
Issues = "https://github.com/kritagyadubey/EzSort/issues"

[tool.setuptools.packages.find]
include = ["ezsort*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["ezsort"]

[tool.coverage.report]
show_missing = true
